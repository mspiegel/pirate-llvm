# RUN: yaml2obj %s -o %t
# RUN: not ld.lld -enclave foo %t -o /dev/null 2>&1 | FileCheck %s

!ELF
FileHeader:
    Class:          ELFCLASS64
    Data:           ELFDATA2LSB
    Type:           ET_REL
    Machine:        EM_X86_64
Sections:
    - Name:         .gaps.strtab
      Type:         SHT_PROGBITS
      Content:      "00666F6F"

# CHECK: .gaps.strtab does not have a null final element

# vim: set filetype=yaml:
